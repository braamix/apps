Port SIMH besm6. The host build is here and boots Unix; what is left is the
Braam target — one output sink, deferred disk/drum I/O, the console behind an
interface, dissolving simh/ into machine.cpp, then proc_main and the two
screens.
