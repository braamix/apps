Port SIMH besm6. The host build is here and boots Unix, with the framework
dissolved and nothing that blocks below cpu_burst(); what is left is the Braam
target — proc_main and the coroutine seams, the package and its data, and the
second screen.
