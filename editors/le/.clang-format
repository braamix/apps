# The tree's own style, less the include sorting: edit.h defines `byte`, `offs`
# and `num` and pulls in the twelve headers written against them, so the order
# of the includes is part of the source rather than a matter of taste.
BasedOnStyle: InheritParentConfig
SortIncludes: Never
