/* Where the data lives, relative to the package's own store directory.
 * _citrus_map_file is the one place that prepends the prefix, which is how
 * every path in the library stays the string literal upstream wrote. */
#ifndef _PATHS_H_
#define _PATHS_H_

#define _PATH_ESDB     "/share/i18n/esdb"
#define _PATH_CSMAPPER "/share/i18n/csmapper"

#endif
