from x import y as z
import w
global g
def f(a, b=1, *c, **d) -> int:
    nonlocal_ = None
    assert a is not None, "boom"
    del b
    try:
        raise ValueError
    except ValueError as e:
        pass
    finally:
        pass
    with open('f') as h:
        yield a
    return True if a else False
class K(object):
    pass
while not (a and b or c):
    continue
for i in x:
    break
else:
    pass
lambda q: q
